'use strict';

/**
 * Crea una funcion que tome array con numeros como argumentos 
 * y determine si la secuencia de numeros es estrictamente 
 * creciente, decreciente o ninguna.
 * 
 * ejemplos:
 * check([1, 2, 3]) ➞ "increasing"
 * check([3, 2, 1]) ➞ "decreasing"
 * check([1, 2, 1]) ➞ "neither"
 * check([1, 1, 2]) ➞ "neither"
 * 
 * @param {Array} array
 * @returns {string}
 */

function check(array) {

}

module.exports = check;
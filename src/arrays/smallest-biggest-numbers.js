'use strict';

/**
 * Crear una funcion que tome un array de numeros y retorne
 * el minimo y maximo numero dentro del array, en ese orden.
 * 
 * Si el array esta vacio retorne null.
 * 
 * ejemplos:
 * minMax([2334454, 5]) ➞ [5, 2334454]
 * minMax([1, 2, 3, 4, 5]) ➞ [1, 5]
 * minMax([1]) ➞ [1, 1]
 * min([]) ➞ null
 * 
 * @param {Array} array
 * @returns {(Array|null)}
 */

function minMax(array) {
  
}

module.exports = minMax;